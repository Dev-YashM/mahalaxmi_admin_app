import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF0C2C55);
  static const Color secondary = Color(0xFF296374);
  static const Color accent = Color(0xFF629FAD);
  static const Color background = Color(0xFFEDEDCE);
  static const Color surface = Colors.white;
}